## Customize the test machine
machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - sbt test:compile
  cache_directories:
    - "~/.ivy2"
    - "~/.sbt/boot/scala-2.10.4"

test:
  override:
    - sbt test

